package it.example.testFabrick.servizi;

import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.client.RestTemplate;

public class ApiFabrick {

    //TODO spostare qua le chiamate api fabrick
/*
    RestTemplate restTemplate;

    static final String defaultUrl= "https://sandbox.platfr.io";

    public ApiFabrick(){
        this.restTemplate= new RestTemplate();
    }

    public String getSaldo(@PathVariable String accountId){
        String uri= defaultUrl + "/api/gbs/banking/v4.0/accounts/" + accountId;
        restTemplate.set
        String result= restTemplate.getForObject(uri, String.class);
        return result;
    }
*/
}
